mutation register($input: LoginInput!) {
  register(input: $input) {
    token
    refreshToken
  }
}

mutation signin {
  signin {
    email
    id
    devices {
      name
      id
      authorized
    }
  }
}
